import { defineConfig, devices } from '@playwright/test'

export default defineConfig({
  testDir: './e2e',
  timeout: 30_000,
  expect: { timeout: 5_000 },
  fullyParallel: true,
  forbidOnly: !!process.env.CI,
  retries: process.env.CI ? 2 : 0,
  workers: process.env.CI ? 1 : undefined,
  reporter: [['html', { open: 'never' }], ['line']],
  use: {
    baseURL: 'http://localhost:3000',
    trace: 'on-first-retry',
    screenshot: 'only-on-failure',
  },
  projects: [
    {
      name: 'chromium',
      use: { ...devices['Desktop Chrome'] },
    },
  ],
  webServer: {
    command: 'npm run dev',
    url: 'http://localhost:3000',
    reuseExistingServer: !process.env.CI,
    timeout: 120_000,
    env: {
      // Auth bypass — only active during Playwright runs, never in production.
      E2E_SKIP_AUTH: 'true',
      // Dummy Supabase values prevent SSR client init errors.
      NEXT_PUBLIC_SUPABASE_URL: 'http://localhost:54399',
      NEXT_PUBLIC_SUPABASE_ANON_KEY: 'e2e-test-anon-key',
    },
  },
})
